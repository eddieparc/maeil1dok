# 사람이 해야 하는 일 — 인증/세션 마이그레이션

에이전트가 코드로 할 수 있는 것은 끝냈다. 이 문서는 **사람만 할 수 있는 것**을 실행 순서대로
적는다. 관측값과 판정은 `docs/auth-migration-metrics.md` 에 기록한다(그 문서가 게이트가
지목하는 기록 대상이다).

각 항목은 **무엇을 · 어떤 기기로 · 무엇을 확인하고 · 어디에 기록** 을 담는다.

---

## 지금 상태

| 축 | 상태 |
|---|---|
| 서버 계측 | **배포 완료. 수집 중** (검증 증거는 metrics 문서 `## 계측 배포 검증`) |
| 웹 오프라인 처리 | **배포 완료.** `unknown-offline` + 중립 배너 |
| 셸 쿠키 파괴 버그 | **코드 수정 완료, 게시 안 됨** |
| 셸 네비게이션 우회 | **코드 수정 완료, 게시 안 됨** |

API·웹은 `bc869579` 로 배포됐다. 셸 변경은 같은 커밋에 들어 있지만 **OTA 로 게시되지
않았다** — `deploy-oci` 는 API·웹만 배포하고 `eas update` 는 CI 에 없다. 게시는 아래
H1 이 끝나야 한다.

**베이스라인 수집이 이미 시작됐다.** 다만 H2(식별 문자열 실측)가 끝나기 전 구간은
헤더 없는 요청이 전부 `unknown` 으로 들어간다 — 7일 베이스라인은 H2 완료 시점부터
세는 것이 맞다.

---

## H1. OTA 실도달 판정 — 이후 모든 순서를 가른다 (최우선)

> **[2026-08-29 최종] H1 판정 완료 = (3-b) 도달 실패. 원인은 채널 부재다.**
>
> 실기기에서 `앱 구버전 — 업데이트 미도달` 이 나왔고, 런타임 `1.2.1` 로 다시 게시해도
> 같았다. 서버에 채널 헤더 없이 물으면 **HTTP 400 "channel-name: Required"** 가 온다.
> `expo prebuild` 는 채널을 심지 않으며(EAS Build 가 하는 일), 스토어 빌드의 출처인
> 로컬 `ios/Expo.plist` 에 `EXUpdatesRequestHeaders` 가 없었다. `eas build:list` 에
> 1.2.x 프로덕션 빌드가 하나도 없다 — 스토어 바이너리는 로컬 빌드 수동 제출이다.
>
> **현재 스토어 앱은 업데이트 확인 때마다 400 을 받는다. 어떤 런타임도 도달하지 않는다.**
>
> **따라서 이후 순서가 바뀐다.** 셸 변경은 OTA 가 아니라 **새 스토어 빌드**로만 나간다.
> H7(애플 계정 전환 예외 승인)·H8(스토어 제출 자격)이 **선행 조건**이 되고,
> `eas.json` 의 제출 자격 자리표시자(`YOUR_APPLE_ID` 등)를 채워야 한다.
>
> 새 빌드는 **채널을 반드시 실어야 한다.** `eas build --profile production` 이 자동으로
> 심는다. 로컬 빌드 경로(`npm run build`)도 이제 prebuild 직후 채널을 자동 주입하고
> 검증한다 — 다만 Xcode 나 gradlew 를 **직접** 부르면 그 자동화를 우회하므로, iOS 는
> Archive 후 제출 전에 `npm run verify:store -- --artifact <경로.ipa>` 를 반드시 돌린다.
>
> 그 빌드가 스토어에 올라가면 **이미 게시된 Part A 셸이 그대로 적용된다.**
> 서버·웹 변경은 이 판정과 무관하게 이미 배포돼 동작 중이다.

**이것을 먼저 하지 않으면 나머지가 무의미하다.** 스토어에 깔린 앱의 런타임 버전을 모르면
OTA 를 게시해도 **어느 기기에도 도달하지 않을 수 있다.** 게시했는데 도달하지 않은 상태는
"고쳤다고 믿지만 아무도 안 고쳐진" 최악의 상태다.

**판정: (3-b) 도달 실패. 이 항목은 닫혔다. 아래는 결과이고, 남은 실행은 H8 이 갖는다.**

**어떻게 판정했나**: 실기기(iOS, 스토어 설치본)에서 계정 설정 하단이 `앱 구버전 — 업데이트
미도달` 로 나왔고, 런타임 후보 `1.2.1` 로 다시 게시해도 같았다. 런타임을 계속 바꿔 보는 대신
업데이트 서버에 **채널 헤더 없이** 물었더니 원인이 나왔다.

```
HTTP 400  "channel-name": Required.
```

**원인은 런타임이 아니라 채널 부재다.** `expo prebuild` 는 채널을 심지 않는다 — EAS Build 가
한다. 그리고 **그 로컬 빌드 경로가 리포에 있다**: `scripts/build.sh` 가 "빌드 환경 선택:
1) 클라우드(EAS Build) / 2) 로컬" 을 묻고, `2` 를 고르면 prebuild → `gradlew bundleRelease`
또는 Xcode Archive 로 간다. 리포에 로컬 서명 자격증명(`credentials/`)이 있고,
`build.sh:update_app_json()` 이 app.json 버전을 sed 로 직접 박는 것도 같은 사실을 가리킨다.
스토어 빌드의 출처인 로컬 `ios/Expo.plist` 에 `EXUpdatesRequestHeaders` 가 없었고
`eas build:list` 는 2026-01 `1.0.1` 에서 멈춰 있다.

**그 경로는 이제 막혔다**: `build.sh` 가 prebuild 직후 채널을 자동 주입하고 검증하며,
Android 산출물은 빌드 직후 검증에서 채널이 없으면 `exit 1` 로 멎는다.

**따라서 현재 스토어 앱은 업데이트 확인 때마다 400 을 받는다. 어떤 런타임으로 게시해도
도달하지 않는다.** 시뮬레이터·에뮬레이터에서 도달했던 것은 **우리가 채널을 직접 심어
빌드했기 때문**이며, 그 실험은 게시 사슬이 정상임을 증명했지 스토어 바이너리가 그 사슬에
올라타 있음을 증명하지 못했다.

**이후 순서가 바뀐다**: 셸 변경은 OTA 가 아니라 **새 스토어 빌드**로만 나간다.
H7·H8 이 선행 조건으로 승격된다. 실행 절차는 **H8 런북**을 본다.

**재발 방지**: `cd mobile && npm run verify:store -- --native` 가 prebuild 직후 채널 부재를
잡고, `-- --artifact <경로>` 가 제출 직전 산출물을 잡는다. 채널 없는 산출물은 exit 1 이다.

**기록 위치**: `docs/auth-migration-metrics.md` 의 `## H1 판정` 절에 근거가 있다.

---

## H2. 식별 문자열 실측 — 베이스라인 수집의 선행 조건

**무엇을**: 셸이 서버에 보내는 User-Agent 4종을 채집한다.

**왜 지금**: 계측의 클라이언트 분류기(`backend/authmetrics/clients.py`)에서
`SHELL_UA_PATTERNS` 를 **의도적으로 비워 뒀다.** iOS 웹뷰 / Android 웹뷰 / iOS 네이티브 /
Android 네이티브가 서로 다른 시그니처를 갖는데, 하나만 보고 만들면 나머지 셋이 조용히
`unknown` 으로 새어 코호트 지표가 왜곡된다. 추측으로 채우면 **그럴듯하지만 틀린 숫자**로
7일 베이스라인이 굳는다.

**어떤 기기로**: iOS·Android 실기기 각 1대 (1.2.2 스토어 앱).

**절차**: 기기에서 (a) 앱을 열어 웹뷰 문서를 로드, (b) 로그아웃 버튼을 눌러 셸의 네이티브
`fetch` 를 발생시킨다. 서버 로그에서 그 두 요청의 UA 값을 그대로 복사한다.

**무엇을 확인**: 4개 문자열이 서로 구별 가능한지. 같으면 그 사실 자체를 적는다.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 식별 문자열 실측` 표.
기록 후 `clients.py` 의 `SHELL_UA_PATTERNS` 를 채우는 것은 에이전트가 할 수 있다.

---

## H3. 쿠키 영속성 24시간 — 확인 전용 앱으로

**무엇을**: 기기가 웹뷰 쿠키를 24시간 보관하는지 본다.

**주의 — 평소 쓰는 앱으로 하면 안 된다.** 현재 스토어 앱에는 고치려는 그 버그가 있어서,
그냥 24시간 돌리면 **버그로 인한 로그아웃을 "기기가 로그인 정보를 보관하지 못한다"로 잘못
읽는다.** 그 오판은 이후 설계 결정을 전부 틀리게 만든다.

**어떤 기기로**: `eas.json` 의 **`diagnostic` 프로필**로 빌드한 확인 전용 앱.
`autoIncrement: false` 로 두었으므로 프로덕션 버전 번호를 소모하지 않는다.
`cd mobile && npx eas build --profile diagnostic --platform ios` (Android 도 동일).
담당자 기기에만 설치한다.

**무엇을 확인**: 로그인 후 24시간 뒤 앱을 재시작했을 때 로그인이 유지되는지.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 쿠키 영속성 검증` 표.

---

## H4. 재시작 반복 검증 (F3)

**무엇을**: 셸 수정이 실제로 버그를 없앴는지 확인한다.

**중요 — 앱을 10분 이상 실제로 쓴 뒤에 해야 한다.** 이 버그는 refresh 토큰이 **회전한 뒤**
재시작에서만 나타난다. 로그인 직후 바로 재시작하면 회전이 없어 **버그가 있어도 통과한다.**
웹앱의 갱신 타이머는 5분 주기이므로 최소 두 번은 돌아야 한다.

**어떤 기기로**: H1 판정에 따라 OTA 를 받은 실기기, 또는 `diagnostic` 빌드.

**절차**: 로그인 → 앱을 10분 이상 전면에서 사용(성경 읽기 등) → 완전 종료 후 재시작.
이것을 **20회** 반복한다.

**무엇을 확인**: 20회 모두 로그인이 유지되는지. 한 번이라도 로그아웃되면 그 회차와 직전
행동을 적는다.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 부분 실패 기록` 에 실패 회차,
성공이면 `## 승격과 도달 확인` 옆에 20/20 을 적는다.

---

## H5. 로그아웃 후 재시작 (F6)

**무엇을**: 로그아웃이 재시작 후에도 유지되는지 확인한다.

**왜 별도 항목인가**: H4 와 반대 방향의 위험이다. 셸이 복원 실패 시 쿠키를 지우지 않게
바꿨으므로(작업 9), **로그아웃이 비영속이 될 위험**이 새로 생겼다. 서버가 핸드오프 코드를
무효화하는 것으로 막았지만(작업 36 API + 세대 CAS) 실기기에서 확인해야 한다.

**절차**: 로그인 → 10분 이상 사용 → **로그아웃 버튼** → 완전 종료 후 재시작.

**무엇을 확인**: 재시작 후 로그인 화면이 나오는지. 로그인된 상태로 돌아오면 **회귀**다.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 부분 실패 기록`.

---

## H6. 호환 격리 테스트 4종

**무엇을**: 셸과 웹의 신·구 조합을 각각 판정한다.

**왜 4종 전부**: 두 번째 조합(신 셸 + 옛 웹)은 브라우저에 옛 화면이 캐시된 사용자에게 실제로
생기는 상태인데, H4·H5 로는 시험되지 않는다.

| # | 조합 | 만드는 방법 |
|---|---|---|
| a | 신 셸 + 신 웹 | OTA 받은 앱 + 현재 배포된 웹 |
| b | 신 셸 + 옛 웹 | OTA 받은 앱에서 웹뷰 캐시를 옛 빌드로 고정 |
| c-1 | 옛 셸 + 신 웹 | OTA 안 받은 앱(또는 스토어 1.2.2) + 현재 웹 |
| c-2 | 옛 셸 + 신 웹, 로그인 흔적 삭제 | c-1 상태에서 앱 데이터 삭제 후 재로그인 |

**무엇을 확인**: 각 조합에서 (1) 로그아웃이 유지되는지 (2) 재시작 후 로그인이 유지되는지.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 호환 격리 테스트` 표.

---

## H7. 애플 계정 전환 예외 승인 — 배포 전에 받아야 한다

**무엇을**: "다른 계정으로 로그인" 을 눌렀을 때 애플은 계정 선택 화면을 띄우는 기술적 방법이
없다. 그 예외를 제품 결정으로 승인받는다.

**왜 배포 전인가**: 배포를 다 한 뒤 승인이 나지 않으면 되돌려야 한다. 구글은 보장되고
카카오는 최선 노력이며 애플만 불가능하다.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 애플 계정 전환 예외 승인` 표.

---

## H8. 새 스토어 빌드 — **지금 최우선**. 셸 수정이 사용자에게 가는 유일한 경로

**무엇을**: iOS·Android 제출 자격이 실제로 유효한지 확인한다.

`mobile/eas.json` 의 `submit.production` 에 **자리표시자가 그대로 있다** (실측):
`appleId: "YOUR_APPLE_ID"`, `ascAppId: "YOUR_APP_STORE_CONNECT_APP_ID"`,
`appleTeamId: "YOUR_APPLE_TEAM_ID"`. 이 값들을 채우지 않으면 제출이 불가능하다.

**하지 말 것**: `npx eas submit` 으로 자격을 검증하지 않는다 — 인증만 확인하는 모드가 없어
실제로 제출되거나 아티팩트 없이 실패한다. `eas credentials` 도 대화형이고 변경을 일으킬 수
있어 쓰지 않는다.

**대신**: ASC API 키로 `GET /v1/apps?filter[bundleId]=<번들ID>` 읽기 전용 조회를 해서 응답
앱 id 가 `ascAppId` 와 같은지 대조한다.

**어디에 기록**: `docs/auth-migration-metrics.md` 의 `## 스토어 제출 자격` 표.

### 런북 — 채널을 실은 새 스토어 빌드

**반드시 EAS 로 빌드한다.** 로컬 `expo prebuild` + Xcode/Gradle 로 만들어 제출하면
이번 사고가 그대로 재발한다 — prebuild 는 채널을 심지 않는다.

1. **제출 자격을 채운다** (`mobile/eas.json` 의 `submit.production.ios`).
   `appleId`, `ascAppId`, `appleTeamId` 가 자리표시자 그대로다. Android 는
   `google-service-account.json` 이 필요하다.

2. **버전을 올린다.** iOS 스토어 현재 버전은 `1.2.2` 다(App Store 공개 조회 실측).
   `mobile/app.json` 의 `expo.version` 을 `1.2.3` 으로 올린다.
   `runtimeVersion.policy = appVersion` 이므로 **런타임도 1.2.3 이 된다** — 이미 게시된
   `1.2.2` 업데이트들은 그 빌드에 도달하지 않지만, **같은 코드가 임베디드로 들어가므로**
   문제되지 않는다. 이후 OTA 는 `1.2.3` 으로 게시한다.

3. **buildNumber/versionCode 충돌을 먼저 확인한다 (BLOCKER).** `appVersionSource: remote`
   라서 EAS 가 원격 카운터를 쓰는데, 실측값이 **iOS buildNumber 10 / Android versionCode 16**
   이다. 그런데 로컬 에뮬레이터에 설치돼 있던 앱이 **versionCode 17** 이었다 — 원격 카운터가
   실제 배포분보다 **낮다**. 그 상태로 빌드하면 Play 가 업로드를 거부한다.
   App Store Connect / Play Console 에서 현재 값을 확인하고, 낮으면 올린다:
   ```bash
   cd mobile
   npx eas build:version:get --platform android
   npx eas build:version:set --platform android   # 스토어 현재값보다 크게
   ```

4. **빌드한다.**
   ```bash
   cd mobile
   npx eas build --profile production --platform ios
   npx eas build --profile production --platform android
   ```
   `eas.json` 의 `production.channel = "production"` 을 EAS 가 바이너리에 심는다.

5. **제출 전에 채널을 검증한다 (이 사고의 재발 차단 지점).**
   ```bash
   cd mobile
   npm run verify:store -- --artifact <다운로드한 .ipa 또는 .aab 경로>
   ```
   `OK ... channel "production"` 이어야 한다. `FAIL` 이면 **제출하지 않는다** — 그 빌드는
   업데이트를 영원히 못 받는다.

6. **제출한다.** `npx eas submit --profile production --platform <p>`

7. **스토어 반영 후 도달을 확인한다.** 앱을 완전 종료·재시작 2회 후 **계정 설정 하단**을 본다.
   - `v1.2.3 · <짧은 id>` 또는 `v1.2.3 · embedded` → 새 빌드가 설치됨
   - `앱 구버전 — 업데이트 미도달` → 아직 옛 빌드. 스토어 업데이트를 받는다.

8. **웹의 강제 안내를 켠다 (선택).** 새 빌드가 **양 스토어에 실제로 올라간 뒤에만**
   `NUXT_PUBLIC_LEGACY_SHELL_ENFORCEMENT=blocking` 으로 바꾼다. 그 전에 켜면 업데이트할
   대상이 없는 사용자를 앱에서 쫓아낸다. 기본값 `notice` 는 배너로만 안내한다.

---

## 순서 요약

```
H1 (OTA 도달 판정)  ✔ 완료 — (3-b) 도달 실패. 원인 채널 부재
 └─ H8 (새 스토어 빌드)  ← 최우선. 셸 수정이 사용자에게 가는 유일한 경로
     └─ H7 (애플 예외 승인)  ← H8 제출 전에 받아야 한다
         └─ 스토어 반영 후 → H2 → 7일 베이스라인 → H4·H5·H6
H3 (쿠키 영속성)     ← 병행 가능. diagnostic 빌드 필요 (이것도 EAS 로 빌드해야 채널이 실린다)
```

**H1 이 (3-a) 였다면 갔을 OTA 경로는 존재하지 않는다.** 그 분기를 기다리지 않는다.

## 에이전트가 셸을 게시하지 않은 이유

계획서 8절이 "웹 먼저, 셸 나중" 을 요구한다. 셸이 먼저 도착하면 옛 웹이 로그아웃을 요청하는데
새 셸은 쿠키를 파괴적으로 지우지 않아 **로그아웃이 비영속**이 된다.

그 순서를 기계가 강제하도록 만들어 뒀다:
- 웹 빌드가 `/_build-marker.json` 에 커밋 SHA 를 심는다.
- `cd mobile && npm run publish:ota -- --requires-web <웹커밋SHA> --platform ios`
  가 배포된 마커를 읽어 불일치면 거부한다.
- 리허설·핫픽스처럼 웹 의존이 없는 게시는 `--no-web-dependency "<이유>"` 로 명시한다.
  둘 중 하나를 반드시 줘야 하며 기본값은 없다 — 건너뛰기가 기본이면 순서 버그가 되살아나고,
  요구가 기본이면 리허설과 핫픽스를 게시할 수 없다.
- `--check-only` 로 실제 게시 없이 모든 검사를 돌려볼 수 있다.
